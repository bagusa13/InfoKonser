# =========================================================
# FILE .HTACCESS OPTIMAL UNTUK INFOKONSER.WEB.ID (V2)
# PERBAIKAN: Kompresi Deflate Fix
# FIXED BY AI: Penambahan Referrer-Policy untuk keamanan
# =========================================================

# --- 1. Dasar & URL ---
DirectoryIndex index.html index.php
RewriteEngine On

# --- 2. Pengalihan (Redirect) ---
# Paksa HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Hapus WWW.
[cite_start]RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC] [cite: 2]
RewriteRule ^(.*)$ https://%1/$1 [L,R=301] [cite: 2]

# --- 3. Header Keamanan ---
<IfModule mod_headers.c>
    Header always append X-Frame-Options SAMEORIGIN
    Header set X-XSS-Protection "1; mode=block"
    Header set X-Content-Type-Options nosniff
    
    # PENINGKATAN KEAMANAN: Menambahkan Referrer-Policy 
    [cite_start]Header set Referrer-Policy "strict-origin-when-cross-origin" 
</IfModule>

# --- 4. Kontrol Cache (Expires) ---
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresDefault "access 1 week"
    
    # Media & Font: 1 tahun
    ExpiresByType image/jpg "access 1 year"
    ExpiresByType image/jpeg "access 1 year"
    ExpiresByType image/gif "access 1 year"
    ExpiresByType image/png "access 1 year"
    [cite_start]ExpiresByType image/webp "access 1 year" 
    ExpiresByType application/x-font-woff "access 1 year"
    ExpiresByType application/x-font-ttf "access 1 year"
    
    # CSS & JS: 1 bulan (untuk mempermudah update)
    ExpiresByType text/css "access 1 month"
    ExpiresByType text/javascript "access 1 month"
    ExpiresByType application/javascript "access 1 month"
</IfModule>

# --- 5. Kompresi Gzip (Deflate) ---
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    # PERBAIKAN: 
    [cite_start]Menggabungkan baris application/javascript [cite: 4]
    [cite_start]AddOutputFilterByType DEFLATE application/javascript [cite: 4]
</IfModule>